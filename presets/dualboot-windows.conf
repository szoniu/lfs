#!/usr/bin/env bash
# LFS TUI Installer Preset — Dual-boot with Windows
# Reuses existing ESP, ext4 root partition.

PARTITION_SCHEME='dual-boot'
FILESYSTEM='ext4'
BTRFS_SUBVOLUMES=''
SWAP_TYPE='zram'
SWAP_SIZE_MIB=''
HOSTNAME='lfs'
TIMEZONE='Europe/Warsaw'
LOCALE='en_US.UTF-8'
KEYMAP='us'
KERNEL_CONFIG='defconfig'
MAKEFLAGS='-j4'
USERNAME='user'
USER_GROUPS='wheel,audio,video,input'
ENABLE_SSH='no'
EXTRA_PACKAGES=''
